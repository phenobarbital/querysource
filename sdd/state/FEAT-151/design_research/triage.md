# Design Research Triage — multiquery-multitenant (FEAT-151)

Model: gpt-5.6-luna · codex-cli 0.156.1 · reasoning_effort=high · exit 0 · 10 suggestions.
All `affected_paths` passed repository containment and `test -e`.

| # | Suggestion (kind) | Disposition | Reason | Landed in |
|---|---|---|---|---|
| S1 | Preserve authorization-before-definition lookup (risk) | CONFIRM | Verified: QueryService enforces `_enforce_owned_slug` (service.py:200-215) before `get_source` (307). Peeking first would leak slug existence to unauthorized callers. Tenant handler now authorizes the parent identity BEFORE `repo.get()` for all three routes and both kinds; single path re-evaluates inside QueryService (allow→allow, accepted). | §2 Overview, §3 M3, §5 AC-9/AC-11, §4 test |
| S2 | Centralize tenant load, error mapping, dispatch (architecture) | CONFIRM | Matches the brainstorm design; one `_prepare()` helper (resolve → authorize → load → classify → stash) shared by query/columns/test_slug, one `DefinitionRepository.get` per request asserted by test. | §3 M3, §4 |
| S3 | Thread the definition through the shared query base explicitly (api) | CONFIRM | Verified: `BaseQuery.__init__` forwards `tenant=` explicitly (base.py:34-41) and everything else via `**kwargs` down to `Connection`; an unhandled `definition` kwarg would be silently swallowed. `AbstractQuery` declares it keyword-only and `BaseQuery` forwards it explicitly. | §3 M1, §6 Edit Sites (base.py:39) |
| S4 | Resolve child PBAC against each child owner (risk) | ESCALATE | Verified: `_preflight_multiquery_owned` resolves every child against `request['qs_tenant']` (multi.py:171-175) while `MultiQS` honors per-child `tenant` overrides (multi/__init__.py:315-324). Real mismatch, but it lives in the INLINE multi path (FEAT-147 behavior) — stored-slug dispatch has `_queries == {}` there. Out of this feature's scope; the new dry-run resolves per-child stores correctly. Human decides whether to fix inline preflight here or in a follow-up. | §8 Q5 |
| S5 | Pure child-resolution/preflight helper shared by execution and dry-run (architecture) | CONFIRM | Extract the tenant rule + store resolution (multi/__init__.py:315-324) into a static `MultiQS.resolve_child_owner()` used by both the execution preflight and `QueryHandler.test_slug`; dry-run never builds ThreadQuery/executors nor calls EXPLAIN (already required). | §3 M1/M4, §6 |
| S6 | Handle multi columns directly, ownership before revealing declaration (api) | CONFIRM | Already designed (definition-aware `QueryHandler.columns`, exact empty-204 headers preserved); S1 ordering now guarantees ownership is checked before the declaration is revealed. Separate HEAD/PATCH tests listed. | §2, §3 M4, §4 |
| S7 | Migration / old-store write policy for columns_definition (risk) | CONFIRM | Verified: `create()`/`upsert()` write every non-None model field (definitions.py:303, 347); a defaulted `[]` would be INSERTed into a store lacking the column and fail. Policy: the repository omits `columns_definition` from writes when it is empty, so only explicit non-empty declarations require the migrated column. `public.queries` gets the documented ALTER TABLE (user decision). | §2 Data Models, §3 M2, §6 Edit Sites, §7 |
| S8 | Exact single-parity and no-double-read contract tests (testing) | CONFIRM | Added: capture `definition=` passed to QS/MultiQS, assert one `repo.get`, identity/revision propagation, header/status parity for single slugs vs QueryService. | §4 |
| S9 | Opt-in integration suite for schema + stored-multi coverage (testing) | CONFIRM | Already in §4; extended with child inheritance/override rows and revision assertions. | §4 |
| S10 | Reject malformed provider=multi payloads with 422 on tenant routes (alternative) | REJECT | User decision (brainstorm Round 2 / spec Q): keep the MultiQS fallback for v3 and scheduler parity. The read-count observation is valid and is recorded as a documented exception: the synthetic single child is its own QS and performs its own load; AC-4 covers the top-level slug only. | §7 gotcha (caveat only) |

Summary: 8 confirmed · 1 rejected · 1 escalated.
